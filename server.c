#include "server.h"

int server_start (int portno) {
	printf("Starting server on port %d\n", portno);

	return 0;
}

int server_accept () {

	// fork process and accept

	return 0;
}

int server_close () {

	return 0;
}
